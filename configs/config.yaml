server:
  http:
    addr: 0.0.0.0:8000
    timeout: 30s
  grpc:
    addr: 0.0.0.0:9000
    timeout: 1s
consul:
  address: consul:8500
  path: app/iam/
vault:
  address: vault:8200
  token: myroot
db:
  address: host=iam_db port=54400 user=me dbname=api password=password sslmode=disable
discovery:
  notifications: discovery:///notifications
